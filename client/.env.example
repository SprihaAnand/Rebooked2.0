# Use the same-origin API in deployed environments. The CRA proxy below routes
# this path to the local Express server during development.
REACT_APP_BASEURL=/api/v1

# Optional: enables the manual Google Identity Services button.
REACT_APP_GOOGLE_CLIENT_ID=
